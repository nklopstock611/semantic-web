cd firstTask/pdf-info-extractor

- First, clone and install the scipdf_parser package from the following repository (it's based on GROBID)
  it's recommended to use a virtual env:

pip install git+https://github.com/titipata/scipdf_parser
python -m spacy download en_core_web_sm

- Open ports and execute server:

docker-compose up -d
python grobid_server.py
